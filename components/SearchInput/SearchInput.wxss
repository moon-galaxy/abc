.search_input {
  height: 90rpx;
  width: 100vw;
  padding: 10rpx;
  background-color: var(--themeColor);
  position: fixed;
  z-index: 100;
}
.search_input .input_navigator {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100%;
  background-color: white;
  border-radius: 15rpx;
  color: #666;
}
.abc {
  position: static;
  display: block;
  height: 90rpx;
  visibility: hidden;
}
